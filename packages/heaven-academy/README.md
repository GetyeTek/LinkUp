# @linkup/heaven-academy
**Maintainer:** LinkUp Academic Experience Team

The Heaven Academy module parses dense textbook schemas, provides structural document rendering, and handles immersive academic examinations. 

### Architecture
- Operates a completely isolated document layout engine (`BookReader`).
- Features a self-contained routing pattern for accessing `ExamPavilion` directly from documents without touching the host router.